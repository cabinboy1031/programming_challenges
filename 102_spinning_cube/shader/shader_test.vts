#version 330 core

layout (location = 0) in vec3 position;

void main(){
  int dx = 0,dy = 0,dz = 0; // For modifying later
  gl_Position = vec4(position.x + dx, position.y + dy, position.z + dz, 1.0);
}
